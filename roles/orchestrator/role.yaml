name: orchestrator
version: 2
summary: reads one brief, declares the children that deliver it, waits for their answers, and writes the summary at the end
model: opus
receives:
  - job
  - context
  - skills
verbs:
  - job.create
  - job.read
  - job.stop
