name: infrastructure-writer
version: 1
summary: writes the infrastructure that hosts a product, opens the pull request for it, and never applies it from a sandbox
model: opus
receives:
  - job
  - context
  - skills
verbs:
  - job.create
  - job.read
