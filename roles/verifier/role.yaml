name: verifier
version: 1
summary: checks that a slice satisfies its contracts and is wired in, not only that its tests are green, and changes nothing
model: sonnet
receives:
  - job
  - context
  - skills
