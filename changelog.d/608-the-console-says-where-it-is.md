**The console says where it is at all times, as an address a person could type.** It sits on the
panel's top edge, beside the view and the count: `jobs(acme/house-bills)[3]`. Four levels deep, a
console that says only which view it is on makes a person guess which project's jobs they are reading.

The address is the workspace, then the project, then the job, which is the same shape `krewe` already
takes on the command line. A job is written as the shortened identifier the listing prints rather than
as its title, because no command takes the title.

The panel rather than the footer, because the command bar and the filter bar draw over the footer.
Those are the two moments somebody is typing at the console, and they are exactly when it used to stop
saying where it was.
