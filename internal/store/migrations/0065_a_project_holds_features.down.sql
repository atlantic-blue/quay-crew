-- Every feature in the system goes with this table. They exist nowhere else: they are not on
-- `projects`, they are not on `project_designs`, and no file on the host holds a copy.
drop table if exists features;
