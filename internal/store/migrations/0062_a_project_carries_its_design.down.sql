-- Every brief and every design body in the system goes with this table. The text exists nowhere
-- else: it is not on `projects`, it is not in `contexts`, and no file on the host holds a copy.
drop table if exists project_designs;
