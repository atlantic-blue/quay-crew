# binaries
/bin/
/out/
*.exe

# env and secrets (secrets are set at runtime, never committed)
.env
.env.*
*.local

# local data volumes (event log, sessions, projections, secrets store)
.data/

# logs
*.log

# os / editor
.DS_Store
.idea/
.vscode/
