name: prompt-analyser
version: 5
summary: Reads every message and hands the session a short brief beside it, never replacing what was typed.
events:
  - on: UserPromptSubmit
    entry: bin/hook
    timeoutSeconds: 20
binaries:
  - claude
