# Copy this to deploy/.env and edit it. Compose reads that file automatically, so every `make up`,
# `make upgrade` and `docker compose` command uses the same configuration, and an upgrade cannot
# quietly bring the stack back as something else.
#
#   cp deploy/env.example deploy/.env
#
# deploy/.env is git ignored. It holds configuration, never secrets: the model token is set at
# runtime with `quay secret set` and never written to disk here.

# The model a turn runs against. "claude-code" runs the real thing on your subscription; "echo" runs
# `echo` inside the sandbox instead, which is what continuous integration uses because it has no
# subscription.
QC_MODEL=claude-code

# The image a session's sandbox runs. Build it with `make sandbox-image`.
QC_SANDBOX_IMAGE=quaycrew-sandbox-claude:local

# Where a workspace's conversations and a project's files are kept. Defaults to ~/.quaycrew/data.
# QC_DATA_HOST=/Volumes/work/quaycrew
